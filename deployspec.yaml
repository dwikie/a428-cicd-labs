version: 0.2
  
  
phases: 
  build:
    commands:      
      - sam build
      - sam deploy --no-fail-on-empty-changeset
      - npm install
      - npm run build
      - aws s3 sync --acl public-read build s3://ci-cd-final-project-neithroid/react-app-build